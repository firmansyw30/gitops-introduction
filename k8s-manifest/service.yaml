apiVersion: v1
kind: Service
metadata:
  name: my-sample-react-app-service
spec:
  type: LoadBalancer # Using LoadBalancer to expose the service externally
  selector:
    app: my-sample-react-app
  ports:
    - port: 80
      targetPort: 80